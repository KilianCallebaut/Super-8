using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathFinding {

    

    public static List<Vector3> findPath(Vector3 location, Vector3 destination)
    {
        List<GameObject> nodeList = LevelManager.Instance.Tiles;

        return new List<Vector3>();

        
    }

    public static List<Vector3> findPath(Vector3 location, Vector3 destination, int numberOfPaths)
    {
        List<GameObject> nodeList = LevelManager.Instance.Tiles;
        return new List<Vector3>();


    }
}
